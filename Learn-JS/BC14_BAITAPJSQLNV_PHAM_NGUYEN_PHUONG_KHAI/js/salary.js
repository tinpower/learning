const calcSalary = () => {
  let title = "";
  if (title === "sếp") {
    return salary * 3;
  }
  if (title === "trưởng phòng") {
    return salary * 2;
  }
  if (title === "nhân viên") {
    return salary;
  }
};
